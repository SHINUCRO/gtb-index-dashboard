import React from "react";
import ReactDOM from "react-dom/client";
import GTBDashboard from "./GTBDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <GTBDashboard />
  </React.StrictMode>
);
